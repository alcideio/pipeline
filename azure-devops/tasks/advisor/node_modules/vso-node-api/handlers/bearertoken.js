// Copyright (c) Microsoft. All rights reserved.
// Licensed under the MIT license. See LICENSE file in the project root for full license information.
"use strict";
var BearerCredentialHandler = (function () {
    function BearerCredentialHandler(token) {
        this.token = token;
    }
    // currently implements pre-authorization
    // TODO: support preAuth = false where it hooks on 401
    BearerCredentialHandler.prototype.prepareRequest = function (options) {
        options.headers['Authorization'] = 'Bearer ' + this.token;
        options.headers['X-TFS-FedAuthRedirect'] = 'Suppress';
    };
    // This handler cannot handle 401
    BearerCredentialHandler.prototype.canHandleAuthentication = function (res) {
        return false;
    };
    BearerCredentialHandler.prototype.handleAuthentication = function (httpClient, protocol, options, objs, finalCallback) {
    };
    return BearerCredentialHandler;
}());
exports.BearerCredentialHandler = BearerCredentialHandler;
